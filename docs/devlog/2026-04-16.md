# April 16th, 2026

## Accomplished
- Created custom SSH config to allow only for public key authentication, disabled root log in
- Router can remain powered on safely because SSH is only accessible internally and password auth is disabled. This also allows me to monitor the temperature, and more importantly the noise, of the router to determine the best location in my home.
- Verified interface names and documented each interface with its intended use
- Began writing bootstrap files and configs including those for SSH and systemd-networkd
- Deployed bootstrap systemd-netowrkd config - verified it and applied it

## Issues
1. Ubuntu 24 appears to come with a default config in `/etc/ssh/sshd_config.d/50-cloud-init.conf` where the only line is `PasswordAuthentication yes` that was overriding the normal config. I first tried modifying the `sshd_config` and it did not work due to this override config.

## Resolutions
1. Renamed the `50-cloud-init.conf` to `50-cloud-init.conf.backup` to preserve it while not allowing it to be part of the active config. Additionally, a new config file, `99-custom.conf` was created and the settings were modified manually there. The `sshd_config` file was left unmodified.

## Decisions Made
- Original `50-cloud-init.conf` was preserved as a backup
- SSH custom config was created
- Added the custom SSH config file to IaC repo at /ssh/99-custom.conf to preserve the exact working configuration
- systemd-networkd will be used over netplan for managing the network interfaces and VLANs

## Next
- Create the deployment user on the router